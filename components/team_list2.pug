span.ts-item-holder
  span.ts-item(style=`background-image: url(${ fields.violet || "/static/img/team1.jpg" })`)
  
unless (fields.teamMembers == null)
  each m in fields.teamMembers
    //- id, image, name, content, type
    if ( m.featured == true )
      span.ts-item-holder
        span.ts-item(style=`background-image: url(${m.image})`)
